{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "region-overlay",
  "description": "Region overlay webview: core event permissions so the wake/hide subscriptions survive webview reuse.",
  "windows": ["screenshot-region"],
  "permissions": [
    "core:default"
  ]
}
