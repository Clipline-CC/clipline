# Nightly 1.0.6 release

Publish PR #209's multi-playback-device audio sources and removal of experimental
per-app audio tracks, plus PR #210's review fixes, from develop merge `d1986634`.
Windows/Ubuntu CI and GitHub review bots pass at the merged head.

- [ ] Read rolling Nightly metadata (1.0.5) and advance Cargo, Cargo.lock and Tauri
  together to 1.0.6.
- [ ] Re-review Microsoft's current Fixed Version selector and runtime notes.
  Rotate the x64 runtime to 153.0.4234.48 and refresh review dates through 2026-10-20.
- [ ] Stage the hash-pinned runtime and verify standalone H.264/Opus playback
  through ended, plus HEVC/AV1 capability gating, before publication.
- [ ] Run runtime preflight, workspace tests and warning-denied Clippy; verify
  the diff from the merged feature contains release metadata/docs only.
- [ ] Push the release commit to develop, verify remote ancestry, then push the
  immutable nightly-v1.0.6 tag at that exact release commit.
- [ ] Watch Nightly Release through public-download/hash/signature verification
  and verify the rolling release target and exactly seven assets.
- [ ] Record publication and evidence in handoff.md and leave Clipline open.

Microsoft review sources:
https://developer.microsoft.com/en-us/microsoft-edge/webview2/ and
https://learn.microsoft.com/en-us/microsoft-edge/webview2/release-notes/runtime/153 .
On 2026-09-20 the live x64 selector offers 153.0.4234.48, replacing the pinned
152.0.4191.62 runtime. The exact CAB is 308,509,880 bytes with SHA-256
`11e8240cb0bc56dcd3e4498907203c251346f65107fe35a3a13e152c7d51c79e`.

The release-only develop commit does not receive PR CI. Local gates and the
tag-triggered release gates must pass; do not label absent develop checks green.
Assets are built and uploaded only by the Nightly workflow. No stable release,
manual asset replacement, forced version tag or manual rolling-tag movement.

Prepublication evidence (2026-09-20): WebView2 and FFmpeg payload preflights
pass; full local workspace tests and warning-denied workspace Clippy pass. A
debug build using the complete standalone configuration ran the staged
153.0.4234.48 runtime from its fixed-runtime path. H.264/Opus, HEVC and AV1
samples each played through ended with 60 presented frames. The product's exact
HEVC and AV1 `canPlayType` probes both returned `probably`, matching successful
playback. The local test entrypoint and media were generated under ignored
`target/` paths and are not release assets; no product source changes were made.
