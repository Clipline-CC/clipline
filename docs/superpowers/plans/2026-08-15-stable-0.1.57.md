# First Stable 0.1.57 Release

> **For agentic workers:** Execute this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for
> tracking and remain unticked by repository convention.

**Goal:** Publish Clipline's first non-prerelease GitHub Stable as 0.1.57, and ship the same bits
to Nightly so current Nightly users receive the Stable channel picker.

**Release-worthy user-visible changes since Nightly 0.1.56:**

- Settings → General → Updates offers **Stable** next to Nightly. The default remains Nightly.
- First immutable `v0.1.57` GitHub release; updater endpoint is `/releases/latest/download/latest.json`.

## Task 1: Release-version the approved merge

- [ ] Confirm PR #168 merged into `develop` after green Ubuntu and Windows checks.
- [ ] Advance the Cargo package, lockfile, and Tauri versions from 0.1.56 to 0.1.57.
- [ ] Re-review the Microsoft WebView2 Fixed Version payload. Keep **151.0.4129.86** (the live
      selector still names that build; Runtime 152 is not current). Refresh `reviewed_on` /
      `review_due_on`.
- [ ] Add this plan and release checkpoint scaffolding.
- [ ] Commit and push the release-only change to `develop`.

## Task 2: Verify release inputs and quality gates

- [ ] Run workspace tests and warning-denied workspace Clippy.
- [ ] Confirm the release commit differs from CI-green merge `283d381` only in version metadata,
      WebView2 review dates, and release documentation.

## Task 3: Fast-forward `main` and publish

- [ ] Fast-forward `main` to the `develop` release commit so the branches are identical.
- [ ] Push immutable tags `v0.1.57` and `nightly-v0.1.57` at that commit.
- [ ] Watch Stable Release and Nightly Release through public-download verification.
- [ ] Confirm `gh release view v0.1.57` is GitHub latest with seven assets, and
      `/releases/latest/download/latest.json` matches the staged manifest.
- [ ] Record publication details in `handoff.md`.
