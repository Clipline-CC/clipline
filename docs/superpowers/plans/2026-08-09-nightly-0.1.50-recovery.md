# Nightly 0.1.50 Recovery

> **For agentic workers:** Execute this plan task-by-task. Steps remain unticked by repository
> convention.

**Goal:** Keep the Nightly build job read-only, stop an optional `gh` release-notes failure from
poisoning the step exit code, then publish the corrected rolling Nightly as 0.1.50 without moving
the failed 0.1.48 or 0.1.49 tags.

- [ ] Add a contract that requires the release-notes fallback to clear the native exit status
      before `prepare-nightly-assets.ps1`, while keeping the build job on `contents: read`.
- [ ] Reset `$LASTEXITCODE` after handling the optional `gh api .../releases/generate-notes`
      failure so a successful asset preparation cannot exit 1 from a stale native status.
- [ ] Parameterize the `docs/release-updates.md` tag example from the Tauri version instead of
      hardcoding `nightly-v0.1.48`.
- [ ] Bump Cargo, Cargo.lock, and Tauri to 0.1.50.
- [ ] Run the WebView2 preflight, focused release/security tests, workspace tests, warning-denied
      Clippy, YAML/actionlint checks, and `git diff --check`.
- [ ] Merge into `develop`, push immutable `nightly-v0.1.50`, and verify all seven public assets.
- [ ] Record both failed Nightly attempts and the successful 0.1.50 publication in `handoff.md`.
