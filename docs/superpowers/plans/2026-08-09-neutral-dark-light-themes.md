# Neutral Dark and Light Themes Plan

## Goal

Add conventional neutral Dark and Light themes alongside Clipline's branded and OLED palettes.

## Tasks

1. Add failing settings and UI-contract coverage for both values, selector options, complete token palettes, and the Light browser color scheme.
2. Add the Dark and Light settings variants/options and define both through the existing CSS theme tokens.
3. Keep Replay, Session, and Trim colors distinct and tint the in-app logo for both themes.
4. Update the design and handoff docs, run workspace tests and clean-cache clippy, then relaunch Clipline for visual verification.
