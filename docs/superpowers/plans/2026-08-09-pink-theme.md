# Pink Theme Plan

## Goal

Add a deep-rose Pink theme that previews immediately, persists through the existing settings transaction, and keeps recording, success, and marker states distinct.

## Tasks

1. Add failing settings and UI-contract coverage for the new theme value, selector option, and complete CSS token palette.
2. Add the Pink settings enum variant and selector option.
3. Define the Pink palette entirely through the existing CSS custom properties.
4. Update the design and handoff docs, run workspace tests and Clippy, then relaunch Clipline for visual verification.
