# Nightly 0.1.48 Release

> **For agentic workers:** Execute this plan task-by-task. Steps remain unticked by repository
> convention.

**Goal:** Publish Clipline 0.1.48 through the tag-triggered rolling Nightly workflow.

- [ ] Confirm PR #145 and the intended product changes are on `develop` with green CI.
- [ ] Bump Cargo, Cargo.lock, and Tauri to 0.1.48.
- [ ] Recheck the reviewed WebView2 Fixed Version metadata and standalone paths.
- [ ] Run the WebView2 preflight, workspace tests, and warning-denied Clippy.
- [ ] Merge the release metadata into `develop` and tag that exact commit `nightly-v0.1.48`.
- [ ] Verify the Nightly workflow, rolling target, and all seven public release assets.
- [ ] Record the published version, commit, URL, and verification in `handoff.md`.
