# Nightly 0.1.49 Recovery

> **For agentic workers:** Execute this plan task-by-task. Steps remain unticked by repository
> convention.

**Goal:** Make standalone FFmpeg verification work in Tauri's legacy Windows PowerShell process,
then publish the corrected rolling Nightly as 0.1.49 without moving the failed 0.1.48 tag.

- [ ] Add a contract that rejects `Get-FileHash` in the standalone FFmpeg verifier.
- [ ] Replace those calls with a local .NET SHA-256 helper compatible with Windows PowerShell.
- [ ] Run the verifier through the exact `beforeBundleCommand` executable and arguments.
- [ ] Bump Cargo, Cargo.lock, and Tauri to 0.1.49.
- [ ] Run the WebView2 preflight, workspace tests, and warning-denied Clippy.
- [ ] Merge into `develop`, push immutable `nightly-v0.1.49`, and verify all seven public assets.
- [ ] Record the failed 0.1.48 attempt and successful 0.1.49 publication in `handoff.md`.
